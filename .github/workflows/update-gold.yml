name: 更新臺銀黃金牌價

on:
  schedule:
    - cron: "40 1,7 * * 1-5"
  workflow_dispatch: {}

permissions:
  contents: write

concurrency:
  group: data-update
  cancel-in-progress: false

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: 抓取金價
        run: python scripts/update_gold.py
      - name: 有變動才 commit
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/gold.json
          if git diff --cached --quiet; then echo "沒有變動"; exit 0; fi
          git commit -m "更新黃金牌價 $(date -u +'%Y-%m-%d %H:%M')"
          git pull --rebase
          git push
